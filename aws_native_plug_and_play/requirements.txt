# Cloud Cost Anomaly Agent — pinned dependencies
# boto3 is provided by the Lambda runtime; pin it here for local dev parity.

boto3==1.38.0
botocore==1.38.0
requests>=2.31.0

# The original Elasticsearch-based agent (root agent.py) also needs:
# elasticsearch==8.17.1
